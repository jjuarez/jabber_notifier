#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), %w{.. lib})
require 'jabber_notifier'

JabberNotifier::Cli.new(Choice.choices[:config]).notify(:to =>Choice.choices[:to], 
                                                        :msg=>Choice.choices[:msg])