#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), %w{.. lib})

require 'jabber_notifier'

JabberNotifier::Cli.run( :contact=>Choice.choices[:contact], :message=>Choice.choices[:message])