#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), %w{.. lib})

require 'jabber_notifier'
require 'jabber_notifier_op'


JabberNotifier::Connection.configure(Choice.choices[:config]).notify(Choice.choices[:to], Choice.choices[:msg])